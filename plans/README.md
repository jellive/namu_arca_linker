# Implementation Plans

Generated by the improve skill on 2026-07-12 (advisor session, planned at
commit `1d55017`). Each executor: read the plan fully before starting, honor
its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                          | Priority | Effort | Depends on | Status |
|------|----------------------------------------------------------------|----------|--------|------------|--------|
| 001  | Cut v1.5.2 and publish the update to the live CWS listing      | P1       | M      | —          | DONE (repo work; Step 7 publish is operator-only) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- The extension is ALREADY live on the Chrome Web Store (v1.5.1, item
  `fhmagpkcdpcnmbihkgdcmabidcmdmpgl`, published 2026-06-12). Plan 001 is an
  **update** release, not a first submission.
- 001's Step 7 (publish) is operator-only — the executor stops after the
  release commit + tag and reports the checklist. This machine currently has
  no CWS credentials (`.env` per `WEBSTORE_CLI_SETUP.md` not set up), so the
  operator chooses: restore the CLI pipeline (Route A) or upload manually via
  the dashboard (Route B).

## Findings considered and rejected

- **First-time store submission plan**: written first, then discovered the
  listing already exists — superseded by the update-release plan.
- **Fixing the mutation-testing baseline / raising coverage before release**:
  release doesn't depend on it; `npm run release` already gates on tests+lint.
- **Rewriting STORE_LISTING.md from scratch**: the existing copy is good;
  only version refs, permissions justification, and feature copy are stale.
