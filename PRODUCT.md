# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

Chrome 확장(Manifest V3). UI 표면이 세 개이며 각각 성격이 다르다 — 아래 Operating Context 참조.

## Users

주 사용자는 **나무위키를 습관적으로 둘러보면서 아카라이브도 하는 한국어 사용자**다.
UI·문서·커밋이 전부 한국어이고 i18n 이 없다 — 비한국어 사용자를 겨냥한 근거는 없다.

상황은 구체적이다: 나무위키 첫 화면의 **실시간 검색어** 위젯에서 처음 보는 낱말을 발견한
순간이다. 나무위키는 그게 왜 뜨는지 설명해 주지 않는다. 하는 일은 "이거 왜 떴지"를 알아내는
것이고, 원래는 그 낱말을 복사해 아카라이브에 가서 검색해야 했다.

## Product Purpose

나무위키 실검 옆에 아카라이브 **실검챈**(`namuhotnow` 채널) 링크를 심어, 궁금한 순간과
설명이 있는 곳 사이의 거리를 한 번의 클릭으로 줄인다. 성공은 사용자가 복사·붙여넣기를
하지 않고 맥락에 도달하는 것이다.

## Positioning

**단순 검색 링크 생성기가 아니라는 점이 핵심이다.** v1.5.0 부터 아카라이브 앱 API
(`/api/app/list/channel/namuhotnow`)를 호출해 실제 최근 실검챈 글 목록을 가져오고,
각 검색어에 대해 제목 정확·부분 일치를 돌린다(`matchThread` in `arca-api.ts`):

- 매칭되는 글이 있으면 **💬 — 그 토론글로 직행**
- 없으면 **🔎 — 검색 폴백**

이 "직행이냐 검색이냐"의 구분이 차별점이다. 언제나 검색 URL 로 보내는 순진한 도구와 갈린다.
(서비스 워커에서 `fetch()`가 User-Agent 를 못 바꾸므로 `declarativeNetRequest` 로 공식 안드로이드
앱 UA 를 흉내 내 호출한다.)

## Operating Context

**표면 네 개의 성격이 다르다. 같은 것으로 취급하면 안 된다.**

| 표면 | 성격 | 제약 |
|---|---|---|
| **삽입 배지** (content script, `namu.wiki` 한정) | 남의 페이지 안에 얹힌다 | 나무위키 디자인을 방해하면 안 된다. 테마도 나무위키(`theseed`)의 `.theseed-light-mode`/`.theseed-dark-mode`를 따라간다 — OS 설정이 아니다 |
| **팝업** | 켜기/끄기 토글 + 감지 개수 | 아주 짧게 열렸다 닫힌다 |
| **옵션 페이지** | 대상 사이트 목록 관리 | 앉아서 설정하는 면 |

배경 서비스 워커는 UI 가 없다(아카라이브 API 호출·매칭).

⚠️ **네 번째 표면(사이드 패널)을 만들었다가 게시 전에 되돌렸다** — 아래 Product Principles 5 참조.
새 표면을 제안할 때 그 이력을 먼저 볼 것.

**삽입 배지가 이 제품의 얼굴이고, 동시에 가장 조심해야 할 자리다** — 남의 사이트에 무단으로
얹히는 UI 라서, 눈에 띄되 방해하지 않아야 한다는 두 요구가 정면으로 부딪힌다.

## Capabilities and Constraints

### 도달 가능

- 나무위키 실검 옆 💬/🔎 스마트 링크 삽입(MutationObserver 로 페이드 갱신)
- 아카라이브 앱 API 조회 + 제목 매칭
- 멀티 사이트 퀵링크(네이버·구글·X·DCInside), 옵션에서 편집

### MV3 제약 — 설계를 강제한 것들

- **영속 백그라운드 페이지가 없다.** 서비스 워커는 필요할 때만 깨어난다.
- 요청 권한은 **두 개뿐이다**: `storage`, `declarativeNetRequest`.
  호스트 권한: `https://namu.wiki/*`, `https://arca.live/*`. **그 이상 요구하지 않는다** —
  `activeTab`·`tabs`·`scripting` 없음. 권한을 늘리는 제안은 Principles 3·5 를 먼저 통과해야 한다.
- CSP: `script-src 'self'; object-src 'none'; base-uri 'none'` — 원격 코드 미사용
  (스토어 신고 내용과 일치).
- 콘텐츠 스크립트는 `https://namu.wiki/*` 에서만, `document_end`.

### 정리 필요 — 사실로 취급하지 말 것

- **`styles.css` 에 죽은 CSS 가 있다**: `.arca-hub` / `.arca-hub-toggle`(우측 도크형
  "실검 인텔리전스 허브" 패널). CHANGELOG 상 **1.4.0 에 추가됐다가 1.5.0 에서 제거**된
  기능의 잔재다. 살아있는 표면이 아니다.
- **README 가 낡았다**: "Chrome 웹스토어에서 설치 (출시 예정)", "현재 제출 준비 중"이라고
  적혀 있으나 **이미 출시됐다**(아래 Evidence). 이 문장을 현재 사실로 인용하지 않는다.

## Brand Commitments

**이름**: `나무위키 실검 아카라이브 링커`. 저장소·패키지명 `namu_arca_linker`.

**아이콘**: `icons/icon16.png` · `icon48.png` · `icon128.png` — 실제 PNG 가 커밋돼 있다
(`icons/README.md`는 아이콘이 없던 시절의 안내문 잔재).

**색 — ⚠️ 통일된 브랜드 색이 없다. 이건 확정된 커밋이 아니라 알려진 격차다:**

| 표면 | 액센트 |
|---|---|
| 팝업·옵션·사이드패널 (라이트) | `--accent: #4a90d9` |
| 팝업·옵션·사이드패널 (다크) | `--accent: #5ba3e8` |
| 삽입 배지 (나무위키 라이트) | 파랑 — 글자·테두리 `#0275d8`, 배경 `#e7f3ff` |
| 삽입 배지 (나무위키 다크) | 보라 — 글자 `#c084fc`, 배경 `#2d1b3d` |
| 켜짐/성공 | `#2ecc71` |

각 표면이 **독립적으로 자기 액센트를 골랐고 공유 토큰 파일이 없다.** 배지의 파랑↔보라 전환은
나무위키 테마를 따라가느라 생긴 것이라 근거가 있지만, 그것과 확장 자체 UI 의 `#4a90d9` 가
왜 다른지는 설계된 이유가 없다. **이걸 정리할지, 정리한다면 어느 색을 정본으로 삼을지는
아직 결정되지 않았다.**

**보이스**: 한국어 전용. 담백하고 정확한 톤, 마케팅 수사 없음. 콘솔 로그 접두사
`[나무위키 아카링커]`.

## Evidence on Hand

**출시돼 있다 — 그리고 실사용자가 있다:**
- 스토어 아이템 ID `fhmagpkcdpcnmbihkgdcmabidcmdmpgl`.
  **2026-08-24 기준 게시 버전 `1.5.3`** (`deploy:status` 의 `crxVersion` 으로 실측).
- 그 직전까지 스토어는 **1.5.1**(2026-06-12 게시)에 두 달 넘게 멈춰 있었다. 1.5.2 의
  견고성 수정이 그동안 사용자에게 가지 않았고, 1.5.3 이 그것까지 함께 실어 보냈다.
- 사용자 수·평점: 내부 릴리스 플랜(`plans/001-cws-v152-update-release.md`)에 **2026-07-12
  시점 42명 / 5.0★** 로 기재돼 있다. ⚠️ **그 시점 스냅샷이고 실시간 확인값이 아니다** —
  현재값으로 인용하지 않는다.
- 빌드 산출물: `release/` 에 v1.5.2 · v1.6.0 · v1.5.3 zip.
  (v1.6.0 zip 은 되돌려진 릴리즈의 잔재이며 게시된 적이 없다.)

**없는 것 — 지어내지 않는다:**
- **스크린샷이 하나도 없다.** `STORE_LISTING.md` §7 이 스크린샷을 "촬영해야 할 목록"으로
  두고 있을 뿐 실제 이미지 파일은 레포 어디에도 없다. 스토어에 올라간 스크린샷은 현재 UI
  대비 낡았을 수 있다고 릴리스 플랜이 스스로 지적한다.
- 후기 · 고객 · 가격(무료, 수익화 코드 없음) · 분석/텔레메트리 없음
  (`PRIVACY.md`가 수집·외부 전송 없음을 명시. 기능상 필요한 두 호스트가 전부)
- i18n 없음(한국어 전용) · React/Vue/Svelte 없음(순수 TS + DOM)
- 표면 간 공유 디자인 토큰/컴포넌트 없음

## Product Principles

1. **남의 페이지에서는 손님이다.** 삽입 배지는 나무위키의 레이아웃·테마·읽기 흐름을
   깨뜨리지 않는다. 눈에 띄는 것과 방해하는 것은 다르다.
2. **직행과 검색을 구분해서 보여준다.** 💬 와 🔎 의 차이가 이 제품의 값어치다.
   둘을 같아 보이게 만들면 평범한 링크 생성기가 된다.
3. **권한은 최소로 유지한다.** `activeTab`·`tabs`·`scripting` 없이 여기까지 왔다.
   기능을 위해 권한을 늘리는 것은 마지막 수단이다.
4. **표면마다 체류 시간이 다르다.** 팝업은 몇 초, 옵션은 앉아서, 사이드패널은 곁눈질,
   배지는 스치듯. 같은 밀도로 설계하지 않는다.
5. **범위를 지킨다 — 만들 수 있다는 것이 만들 이유는 아니다.** 2026-08-24, 사이드 패널 ·
   5분 주기 스냅샷 알람 · 변화 배지(구 1.6.0)를 **완성된 상태에서 되돌렸다.** 동작하지 않아서가
   아니라, "실검 옆에 링크를 붙인다"는 이 확장의 일에서 벗어났기 때문이다. 표면 하나와 권한
   두 개를 더 받을 만한 일이 아니었다. 기능을 더하자는 제안은 이 기준을 먼저 통과해야 한다.

## Accessibility & Inclusion

**최소한이며, 약속된 기준이 아니다.**

- CHANGELOG 1.0.0 이 "접근성 고려 (포커스 상태, outline)"를 기록했고, `styles.css` 가 실제로
  `.arca-link` 의 `:focus` outline 을 라이트/다크 양쪽에 구현한다.
- 그 외 ARIA 속성 · `role=` 사용 · 접근성 정책 문서 · WCAG 목표 · 키보드 내비게이션 명세는
  **전부 없다**(`aria-`, `role=`, `접근성`, `a11y` 전수 검색 결과 위 두 건뿐).
- `STORE_LISTING.md` §3 의 "접근성"은 스토어 **카테고리 후보로 검토했다가 기각**한 것이며
  (최종 선택은 "생산성") 디자인 약속이 아니다.

**적용:** 삽입 배지는 남의 페이지의 탭 순서에 끼어드는 요소다. 접근성을 손볼 때 이 자리가
가장 먼저다.
