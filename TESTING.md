# 테스트 가이드

## 로컬 테스트 방법

### 1. 익스텐션 로드

1. Chrome 브라우저에서 `chrome://extensions/` 접속
2. 우측 상단 **"개발자 모드"** 활성화
3. **"압축 해제된 확장 프로그램 로드"** 클릭
4. 프로젝트 폴더 선택

### 2. 나무위키 접속

1. [https://namu.wiki](https://namu.wiki) 메인 페이지 접속
2. 실시간 검색어 섹션 확인
3. `F12` 를 눌러 개발자 도구 열기
4. **Console** 탭에서 로그 확인

### 3. 예상 로그

정상 작동 시:
```
[나무위키 아카링커] 익스텐션 시작
[나무위키 아카링커] 선택자 "[class*="realtime"] li a"로 10개 항목 발견
[나무위키 아카링커] 10개 항목에 링크 추가 완료 (선택자: [class*="realtime"] li a)
[나무위키 아카링커] 컨테이너 발견: [class*="realtime"]
[나무위키 아카링커] MutationObserver 설정 완료
```

실검을 찾지 못한 경우:
```
[나무위키 아카링커] 익스텐션 시작
[나무위키 아카링커] 실시간 검색어를 찾을 수 없습니다.
[나무위키 아카링커] 개발자 도구(F12)로 DOM 구조를 확인하고 선택자를 업데이트해주세요.
```

## 기능 테스트 체크리스트

### 기본 기능

- [ ] 나무위키 메인 페이지에서 실검 감지
- [ ] 각 실검 항목 옆에 "왜?" 링크 표시
- [ ] 링크 클릭 시 새 탭에서 아카라이브 페이지 열림
- [ ] 올바른 검색어로 아카라이브 검색됨

### 동적 업데이트

- [ ] 페이지 새로고침 시 링크 재생성
- [ ] 실검 갱신 시 자동으로 링크 업데이트 (MutationObserver)
- [ ] 중복 링크가 생성되지 않음

### UI/UX

- [ ] 링크 스타일이 나무위키 디자인과 조화로움
- [ ] 호버 효과 정상 작동
- [ ] 다크모드 대응 (Chrome 설정에서 확인)
- [ ] 모바일 반응형 (모바일 크롬에서 테스트 권장)

### 에러 처리

- [ ] 실검이 없는 페이지에서도 에러 없이 작동
- [ ] 네트워크 오류 시 정상 작동
- [ ] 잘못된 선택자에 대한 폴백 처리

## DOM 구조 확인 방법

실검을 찾지 못하는 경우, 나무위키의 실제 DOM 구조를 확인해야 합니다.

### 1. Elements 탭에서 찾기

1. 나무위키 메인 페이지에서 `F12` 키를 누릅니다.
2. **Elements** 탭을 선택합니다.
3. 좌측 상단의 **"요소 선택"** 버튼 (화살표 아이콘) 클릭
4. 실시간 검색어 중 하나를 클릭합니다.
5. 개발자 도구에서 해당 요소가 하이라이트됩니다.

### 2. HTML 구조 분석

다음과 같은 정보를 확인하세요:

- **컨테이너**:
  ```html
  <div class="실검-컨테이너-클래스">
    <ul>...</ul>
  </div>
  ```

- **리스트 항목**:
  ```html
  <li>
    <a href="/w/검색어">검색어</a>
  </li>
  ```

- **클래스명**: `class="..."` 속성 값
- **ID**: `id="..."` 속성 값

### 3. 선택자 업데이트

확인한 클래스명으로 `content.js`의 `REALTIME_SELECTORS` 배열을 업데이트하세요:

```javascript
const REALTIME_SELECTORS = [
  // 예시: 실검 컨테이너 클래스가 "trending-box"이고 링크가 li > a 구조인 경우
  '.trending-box li a',

  // 기존 선택자들...
  '[class*="realtime"] li a',
  // ...
];
```

### 4. 익스텐션 새로고침

1. `chrome://extensions/` 페이지로 이동
2. "나무위키 실검 아카라이브 링커" 카드에서 🔄 버튼 클릭
3. 나무위키 페이지를 새로고침 (`F5` 또는 `Cmd+R`)

## 디버깅 팁

### 1. 콘솔 로그 활성화

`content.js`의 모든 `console.log`는 개발 중 디버깅에 유용합니다.
프로덕션 빌드 시 제거하거나 조건부로 활성화할 수 있습니다.

### 2. 선택자 테스트

개발자 도구 Console에서 직접 테스트:
```javascript
// 선택자가 요소를 찾는지 테스트
document.querySelectorAll('[class*="realtime"] li a').length
// 결과가 0보다 크면 선택자가 작동하는 것
```

### 3. MutationObserver 디버깅

실검 갱신이 감지되지 않는 경우:
```javascript
// Console에서 직접 실행하여 컨테이너 확인
document.querySelector('[class*="realtime"]')
// null이 나오면 선택자를 업데이트해야 함
```

### 4. 아카라이브 URL 테스트

```javascript
// Console에서 URL 생성 함수 테스트
const keyword = "테스트";
const url = `https://arca.live/b/breaking?keyword=${encodeURIComponent(keyword)}`;
console.log(url);
// 브라우저에서 URL을 직접 열어 확인
```

## 성능 테스트

### 1. 메모리 사용량

1. `chrome://extensions/` → **"백그라운드 페이지"** 클릭
2. **Memory** 탭에서 메모리 사용량 확인
3. 정상적인 경우 1-2MB 이하

### 2. CPU 사용량

1. **Performance** 탭에서 프로파일링 시작
2. 나무위키 페이지 탐색
3. 프로파일링 중지
4. CPU 사용량이 과도하지 않은지 확인

## 크로스 브라우저 테스트

Chrome Extension은 기본적으로 Chrome에서 작동하지만, 다음 브라우저에서도 테스트 가능:

- **Microsoft Edge** (Chromium 기반)
- **Brave Browser**
- **Opera** (Chromium 기반)

각 브라우저의 확장 프로그램 페이지에서 동일한 방법으로 로드할 수 있습니다.

## 버그 리포트

버그를 발견한 경우, 다음 정보를 포함해 이슈를 제보해주세요:

1. **브라우저 및 버전**: Chrome 120.0.0.0
2. **운영체제**: macOS 14.0
3. **재현 단계**:
   - 나무위키 메인 페이지 접속
   - 실검 클릭
   - 예상 결과: 링크 표시
   - 실제 결과: 링크 없음
4. **콘솔 로그**: (개발자 도구 Console 내용)
5. **스크린샷**: (가능한 경우)

---

테스트 완료 후, 모든 기능이 정상 작동하면 프로젝트 완성! 🎉
